# Uncomment the next line to define a global platform for your project
  platform :ios, '16.4'

target 'Consumer' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  #pod 'CRFramework', :path => '../CRFramework'
  pod 'CRFramework', :git => 'https://github.com/redangelboy/CRFramework.git', :tag => '1.0.2'

  # Pods for Consumer

end
